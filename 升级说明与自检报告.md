# 青阅书城 Web 前端大作业 · 增量升级修改清单与自检报告

> 班级：软工2507班  学号：8002125223  姓名：邹浏源
> 本次为**纯增量升级**，未改动既有目录结构与命名规范，所有页面仍可双击 `index.html` 以 `file://` 协议直接运行。

## 一、新增功能总览

| 类别 | 功能 | 实现位置 |
|---|---|---|
| A1 | 全站明暗主题切换（太阳/月亮按钮、localStorage 持久化、0.3s 过渡） | base.css / common.css / common.js |
| A2 | 回到顶部按钮 + 顶部阅读进度条 | common.css / common.js |
| A3 | 滚动进入动画（fadeInUp + IntersectionObserver） | common.css / common.js + 各页 section |
| A4 | 卡片 3D 倾斜 / 按钮波纹 / 图片懒加载 | common.css / common.js + 各页卡片渲染 |
| B1 | 图书快速预览弹窗（封面/价格/评分/标签/简介/加购/详情） | common.css / common.js + 各页卡片 |
| B2 | 图书对比（最多 3 本、对比栏、对比表格、最优值标绿） | books.html / books.css / books.js |
| B3 | 优惠券系统（满减/折扣券、实时优惠计算、localStorage 记忆） | data.js / cart.html / cart.css / cart.js |
| B4 | 搜索自动补全（书名匹配、高亮、键盘上下/Enter/Esc、点击外部关闭） | common.js + books.html / books.css |
| B5 | 订单详情 + 物流时间线（4 节点、当前高亮、已完成打勾） | data.js / user.html / user.css / user.js |
| B6 | 图书数据 16 → 24 本 + 8 个 SVG 封面 | data.js / assets/images/goods/book-17~24.svg |
| C1 | 限时秒杀横幅（倒计时、横向滚动、秒杀价划线、抢光进度条） | index.html / index.css / index.js |
| C2 | 编辑推荐书单（书单卡片 + 展开 modal） | data.js / index.html / index.css / index.js |
| C3 | 轮播图触摸滑动（touchstart/move/end，>50px 切换） | index.js |

## 二、修改 / 新增文件清单

### JS
- `js/data.js`：新增图书 id 17~24；新增 `COUPONS`、`calcCouponDiscount()`、`EDITOR_PICKS`、`ORDERS`、`ORDER_STATUS_MAP`。
- `js/common.js`：`STORAGE_KEYS` 新增 `DARK_MODE`、`COUPON`；新增 `applyDarkMode/initTheme/initScrollTools/initScrollAnimations/initCardTilt/initRipple/initLazyLoad/showBookPreview/initSearchAutocomplete`；`initLayout()` 内统一调用。
- `js/page/index.js`：卡片改造（card-tilt + data-src + 快捷操作）；新增秒杀、编辑推荐、轮播触摸。
- `js/page/books.js`：卡片改造；新增对比状态/对比栏/对比 modal、页面搜索自动补全。
- `js/page/cart.js`：Vue 增加优惠券 data/computed/watch，结算明细与实付金额，订单写入优惠金额。
- `js/page/user.js`：订单合并示例数据；新增 `showOrderDetail()`（商品明细 + 订单信息 + 物流时间线）。
- `js/page/detail.js`：相关推荐卡片接入 data-src 懒加载与快速预览。

### CSS
- `css/base.css`：`:root` 之外新增 `[data-theme="dark"]` 暗色变量、主题切换过渡、暗色 body 渐变。
- `css/common.css`：暗色表面覆盖、主题按钮、回到顶部、进度条、fadeInUp、card-tilt、波纹、懒加载、快速预览 modal、卡片悬浮操作、搜索建议。
- `css/page/index.css`：秒杀、编辑推荐书单、响应式。
- `css/page/books.css`：页面搜索框、对比栏、对比表格、最优值高亮。
- `css/page/cart.css`：优惠券选择面板、结算明细。
- `css/page/user.css`：订单详情、物流时间线。

### HTML
- `index.html`：新增秒杀模块、编辑推荐模块；主要 section 加 `animate-on-scroll`。
- `books.html`：新增页面搜索框、对比栏容器。
- `cart.html`：新增优惠券选择面板、结算明细行。
- `user.html`：订单卡片新增"查看详情"按钮。

### 资源 / 工具
- 新增 `assets/images/goods/book-17.svg` ~ `book-24.svg`（Python 生成，风格与原封面一致）。
- 新增 `_tools/gen_covers_new.py`。

## 三、关键设计决策（与任务描述的两处偏差说明）

1. **明暗模式未复用 `STORAGE_KEYS.THEME`**：原项目 `user.js` 已把 `THEME` 用作"主题色 hex"（强调色切换）。为**不破坏现有偏好设置功能**，明暗模式改用新键 `STORAGE_KEYS.DARK_MODE = 'qy_dark_mode'`，二者互不干扰。
2. **主题按钮用 JS 注入而非逐个改 HTML header**：在 `initTheme()` 中统一注入到 `.header-tools`，保证 12 个页面按钮一致、零遗漏，效果与手写相同。

## 四、自检结果

| # | 检查项 | 结果 |
|---|---|---|
| 1 | 7 个改动 JS 文件 `node --check` 语法校验 | 通过 |
| 2 | 图书数据 24 本、封面 24 个一一对应 | 通过 |
| 3 | 12 个 HTML 均含 `.header-tools` 且页面 JS 均调用 `initLayout`（全站升级生效） | 通过 |
| 4 | 明暗主题按钮注入、变量/表面暗色覆盖、localStorage 持久化 | 代码已实现 |
| 5 | 回到顶部 + 进度条（>300px 显示、滚动更新） | 代码已实现 |
| 6 | 快速预览弹窗（各页卡片眼睛按钮） | 代码已实现 |
| 7 | 图书对比（选 3 本 → 表格 → 最优值标绿） | 代码已实现 |
| 8 | 优惠券（选券 → 优惠金额 → 实付，记忆选择） | 代码已实现 |
| 9 | 搜索自动补全（书名高亮/键盘/Esc/外部关闭） | 代码已实现 |
| 10 | 订单详情 + 物流时间线（4 节点高亮） | 代码已实现 |
| 11 | 限时秒杀倒计时（到当天 24:00，结束显示已结束） | 代码已实现 |
| 12 | 编辑推荐书单卡片 + 展开 modal | 代码已实现 |
| 13 | 轮播触摸滑动（>50px 切换，保留自动播放/指示器） | 代码已实现 |
| 14 | file:// 协议直接双击运行、无服务器依赖 | 通过（无 fetch/模块，全部相对路径） |

> 说明：本次运行环境无桌面浏览器自动化能力，浏览器控制台实测项请在本机双击 `index.html` 复点一遍；上述"代码已实现"项均经语法校验与静态走查，逻辑闭环。
